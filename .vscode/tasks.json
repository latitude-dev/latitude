{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Latitude Extension",
      "type": "shell",
      "command": "pnpm build",
      "options": {
        "cwd": "${workspaceFolder}/tools/latitude-vscode"
      },
      "problemMatcher": "$tsc",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}